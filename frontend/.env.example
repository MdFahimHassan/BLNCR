# Copy to `.env` (or `.env.production` for a Vercel build) and set to the
# deployed backend URL. Locally this defaults to http://localhost:9090 even
# without a .env file — see frontend/src/api/client.js.
VITE_API_BASE_URL=http://localhost:9090
